<?php

class Instagram_userTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('instagram_user')->truncate();

		$instagram_user = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('instagram_user')->insert($instagram_user);
	}

}
